node_modules
.next
.env.local
results
*.tsbuildinfo

# scripts/laya_server.py's local venv — a few hundred MB of torch, never committed
.venv

# local tool state, not part of the app
.headroom
.tokensave
.claude
.graphify
